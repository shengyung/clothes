# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# FORMA — 環境變數範本
# 複製此檔案為 .env，並填入實際值
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── 資料庫 ──────────────────────────────────────────────
DATABASE_URL=sqlite:///./tryon.db
# 升級 PostgreSQL 時改為：
# DATABASE_URL=postgresql://user:password@localhost:5432/tryon

# ── JWT（必填）──────────────────────────────────────────
# 產生方式：openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-32-char-secret

# ── CORS ────────────────────────────────────────────────
# 逗號分隔的允許來源網域。生產環境用 docker-compose.prod.yml 覆蓋成 https://shapeonyou.io，
# 這裡的值只在本地開發生效。
CORS_ORIGINS=http://localhost:3000

# ── 圖片儲存 ────────────────────────────────────────────
# 本地開發用 MinIO（USE_S3=false），生產用 AWS S3（USE_S3=true）
USE_S3=false

# MinIO（本地開發）
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=tryon
MINIO_USE_SSL=false

# AWS S3（生產，USE_S3=true 時填入）
# S3_REGION=ap-northeast-1
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_S3_BUCKET=vtr-prod-你的名字

# ── Fashn.ai 推論 ───────────────────────────────────────
# 取得：https://app.fashn.ai/api
FASHN_API_KEY=your_fashn_api_key_here

# ── Email（忘記密碼功能）────────────────────────────────
# 取得：https://resend.com/api-keys
# 若留空，忘記密碼功能不會寄信（但其他功能不受影響）
RESEND_API_KEY=
FRONTEND_URL=http://localhost:3000

# ── NextAuth（SSO 社群登入）────────────────────────────
# 產生方式：openssl rand -hex 32
NEXTAUTH_SECRET=change-me-to-another-random-32-char-secret
NEXTAUTH_URL=http://localhost:3000

# Google OAuth（取得：https://console.cloud.google.com/）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# LINE Login（取得：https://developers.line.biz/）
LINE_CLIENT_ID=
LINE_CLIENT_SECRET=

# Facebook Login（取得：https://developers.facebook.com/）
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Apple Sign In（取得：https://developer.apple.com/）
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=
